class IconConfig {
  static const base = "assets/icons/";
  static const format = ".svg";

  static const ellipsis = "${base}ellipsis-vertical$format";
  static const arrowRight = "${base}arrow-right$format";
  static const menu = "${base}menu$format";
  static const mic = "${base}mic$format";
  static const paperClip = "${base}paperclip$format";
}
